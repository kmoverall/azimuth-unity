using UnityEngine;
using System.Collections;

public class SeasonEmitter : MonoBehaviour {
    SphereCollider c_effectBounds;

	// Use this for initialization
	void Start () {
        gameObject.GetComponent<SphereCollider>();
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
